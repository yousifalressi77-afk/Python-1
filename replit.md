# 🤖 PrimeBot - Dashboard Completo

## 📋 مقدمة
بوت Discord احترافي مع داشبورد كامل متكامل (مثل Probot تماماً) مع دعم اللغة العربية الكاملة.

**المالك:** ALSHARQI

---

## ✨ الميزات الرئيسية

### 1️⃣ Discord Bot (45 أمر)
- **الفئات:**
  - 🎮 ألعاب (10 أوامر)
  - 📊 معلومات (8 أوامر)
  - 🛡️ إدارة (12 أوامر)
  - 🎉 أحداث (7 أوامر)
  - ⚙️ عام (8 أوامر)

- **نظام التكاتة المتقدم:**
  - Select Menu (قائمة اختيار) بدل الأزرار
  - تخصيص الخيارات
  - أزرار استقبال وإضافة عضو وإغلاق
  - Embed جميلة ومرتبة

- **أوامر الرسائل:**
  - `/sendmessage` - إرسال رسالة عادية
  - `/sendembed` - إرسال رسالة بـ Embed احترافية

### 2️⃣ Dashboard Web (لوحة تحكم فخمة)

#### نظام التسجيل
- ✅ تسجيل الدخول عبر Discord OAuth2
- ✅ عرض السيرفرات التي أنت أدمن فيها
- ✅ جلسات آمنة

#### 10 صفحات إعدادات:
1. **✉️ نظام الترحيب** - رسالة ترحيب عند دخول عضو
2. **👋 نظام المغادرة** - رسالة وداع عند مغادرة عضو
3. **🎭 الأدوار التلقائية** - رول عند الدخول ورول للبوتات
4. **🛡️ الإدارة** - قناة السجلات ورتبة الميوت
5. **🎫 نظام التكاتة** - إعدادات نظام التكاتة
6. **📋 السجلات** - اختيار قنوات اللوق
7. **🚫 مكافحة الإساءة** - حد الرسائل والفترة الزمنية
8. **⌨️ الأوامر المخصصة** - إضافة أوامر جديدة
9. **⚙️ الإعدادات العامة** - البادئة واللغة
10. **📊 إحصائيات البوت** - معلومات عن البوت

#### قاعدة البيانات
- JSON للإعدادات
- JSON للتكاتة
- JSON للردود التلقائية والاختصارات

---

## 🚀 كيفية الاستخدام

### تشغيل البوت
```bash
npm install
node src/bot.js
```

### تشغيل الداشبورد
```bash
node src/dashboard/server.js
```

### الوصول للداشبورد
1. اذهب إلى `http://localhost:5000`
2. اضغط "تسجيل الدخول عبر Discord"
3. اختر السيرفر وأدر إعدادات البوت

---

## 🛠️ المتطلبات المتقدمة

### لتفعيل OAuth2 (تسجيل الدخول):
1. اذهب إلى Discord Developer Portal
2. اضغط "New Application" وأسميها "PrimeBot"
3. اذهب إلى "OAuth2" > "General"
4. Copy: `DISCORD_CLIENT_ID`
5. Copy: `DISCORD_CLIENT_SECRET`
6. اذهب إلى "OAuth2" > "Redirects" وأضف:
   - `http://localhost:5000/callback` (Development)
   - `https://your-replit-domain.replit.dev/callback` (Production)

### متغيرات البيئة المطلوبة:
```
DISCORD_TOKEN=your_bot_token
DISCORD_CLIENT_ID=your_client_id
DISCORD_CLIENT_SECRET=your_client_secret
SESSION_SECRET=your_session_secret
```

---

## 📁 الهيكل

```
src/
├── bot.js                      # البوت الرئيسي
├── deploy-commands.js          # نشر الأوامر
├── commands/                   # جميع الأوامر (45 أمر)
├── events/                     # الأحداث
├── utils/
│   ├── ticketdb.js            # قاعدة بيانات التكاتة
│   ├── settingsdb.js          # قاعدة بيانات الإعدادات
│   ├── dashboarddb.js         # قاعدة بيانات الداشبورد
│   └── auth.js                # نظام OAuth2
└── dashboard/
    ├── server.js              # خادم الداشبورد
    ├── routes.js              # المسارات الرئيسية
    └── dashboard-routes.js    # صفحات الإعدادات
data/
├── tickets.json               # بيانات التكاتة
├── settings.json              # الإعدادات
└── dashboard.json             # إعدادات الداشبورد
```

---

## 🎯 الحالة الحالية

✅ البوت كامل وشامل (45 أمر)
✅ نظام التكاتة متقدم
✅ الداشبورد فاخر (10 صفحات)
✅ نظام OAuth2 جاهز
✅ قاعدة بيانات JSON
✅ دعم اللغة العربية الكامل

⏳ **في الانتظار:** 
- DISCORD_CLIENT_SECRET من المستخدم (لتفعيل التسجيل الكامل)
- اختبار البوت الكامل

---

## 📝 الملاحظات

- جميع التواصلات بالعربية
- التصميم احترافي مثل Probot
- قاعدة البيانات JSON (يمكن تحويلها إلى MongoDB لاحقاً)
- جميع الأوامر مع Slash Commands
- نظام الجلسات آمن

---

## 🔗 الروابط المهمة

- Bot Invite: `https://discord.com/api/oauth2/authorize?client_id=YOUR_CLIENT_ID&permissions=8&scope=bot%20applications.commands`
- Dashboard: `http://localhost:5000`
- Developer Portal: `https://discord.com/developers/applications`
